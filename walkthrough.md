# إتمام عملية إصلاح "فورمات المصنع" (Factory Reset)

لقد تم بنجاح تطبيق الكود البرمجي الذي يمنحك السيطرة الكاملة والمطلقة على محو قاعدة بيانات المنشأة مع الحفاظ على القواعد التنظيمية للإدارة.

### 🔧 ما تم إنجازه:

1. **دعم المدير العام (Owner):** 
   الآن لم يعد النظام يرفض العملية بحجة (غير مصرح). الكود يسمح لمنصب الـ `admin` و `owner` باستخدام زر المسح.
   
2. **بروتوكول حماية الصلاحيات:**
   تم إعادة صياغة خوارزمية الحذف (TRUNCATE). الخوارزمية ستقوم بمسح جميع الجداول بضربة واحدة لكنها **ستتجاهل جدول `user_permissions` بالكامل**. بعدها مباشرة يتم اختيارياً حذف المستخدمين من غير صنف (الإدارة العليا) لتبقى شاشات وصلاحيات حسابك المالك نظيفة وموجودة بدون انهيار النظام.

3. **مسح بيانات السجل وملفات المنشأة:**
   بناءً على طلبك، تم إدراج كود `UPDATE` لجدول `companies` وجدول `branches`. عند المسح، يتم تحويل اسم المؤسسة إلى "مؤسسة جديدة" والعناوين، والأرقام الضريبية، ولوجو المنشأة (الشعارات) ترجع كلها إلى فوارغ وشكل النظام الخام `NULL`.

4. **بنية تحتية مانعة لشلل النظام:**
   تم إضافة כود `Upsert` يعالج خطأ شاشة نقاط البيع. بعد الفرمتة، سيقوم السيرفر بصمت تام بضخ الأصناف الإجبارية (عميل نقدي برقم 1، مستودع رئيسي برقم 1، وحدة حبة برقم 1) لضمان عدم حدوث خطأ 500 عندما يرغب المحاسب ببدء العمل مجدداً.

---
الشفرة البرمجية موجودة حالياً محلياً على السيرفر الخاص ببيئة التطوير (VS Code). يمكنك رفع التعديل لخوادم (N11) من خلال أوامر الرفع المعتادة الخاصة بك لتجربتها حياً.
