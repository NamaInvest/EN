# GLOBAL READINESS MASTER BASELINE — PHASE 0

## 1. الهدف

تأسست هذه الوثيقة كBaseline كلي للجاهزية العالمية (**Global Readiness Master Baseline**) لمشروع **Nama Invest ERP**. تهدف هذه المرحلة التخطيطية (المرحلة 0) إلى تجميع وحصر الوضع الحالي للأنظمة برمجياً استناداً إلى نتائج الفحوصات الحية اللحظية، ورسم خارطة الطريق المعمارية والعملية التفصيلية التي تنقل النظام من حالة قاعدة الأدلة المؤقتة إلى مستوى الجاهزية التشغيلية للمؤسسات الكبرى (Enterprise-grade) والمنافسة العالمية (World-class)، مع تأسيس مجلد ذاكرة المشروع الموحد `.ai-brain/` كمرجع وحيد للحقيقة لكافة الوكلاء البرمجيين والمطورين.

---

## 2. النطاق

يشمل النطاق التحليلي والتخطيطي لهذه المرحلة مراجعة وتوثيق الفئات البرمجية والتشغيلية التالية:
1. **جودة البيئة والاختبارات (Quality / Testing)**
2. **الخدمات الخلفية والـ APIs (Backend / API)**
3. **مخطط قاعدة البيانات (Database Schema)**
4. **صفحات وواجهات المستخدم (Frontend / UX)**
5. **طبقة الأمن والخصوصية (Security & PDPL)**
6. **عزل وتأمين سياق المستأجرين (Tenant Isolation)**
7. **الحوكمة المالية والقيود المحاسبية (Financial Governance)**
8. **الامتثال للأنظمة والتشريعات الخليجية والسعودية (Saudi Compliance)**
9. **عمليات التشغيل والبناء والنشر (DevOps / CI/CD)**
10. **الأداء واستقرار قواعد البيانات (Performance & Scalability)**
11. **التوثيق ودعم العملاء والجاهزية القانونية والتنظيمية (Docs, Legal & Support)**

---

## 3. ما تم فحصه

تم فحص ومراجعة المكونات البرمجية والأدلة الحية التالية خلال هذه المرحلة:
- **ملفات إعداد وتكوين بيئة الفحص**: `package.json`, `jest.config.ts`, `vitest.config.ts`, `tsconfig.json`, `playwright.config.ts`.
- **ملف مخطط قاعدة البيانات الرئيسي**: `prisma/schema.prisma` ونماذج التوحيد والاستبعادات المعتمدة.
- **مسار الاعتماد والترحيل المالي**: كود محرك الترحيل [src/lib/consolidation-elimination-posting.ts](src/lib/consolidation-elimination-posting.ts) ومسار API الخاص بالاعتماد.
- **نتائج تشغيل أدوات الفحص الحالية**: مخرجات أوامر `npx tsc --noEmit`, `npm run lint`, `npx vitest run`, و `npx jest --runInBand --forceExit` المباشرة والمسجلة في الملحقات.

---

## 4. ما لم يتم فحصه

- **خوادم الإنتاج والـ PM2 الحية**: لم يتم الاتصال بالخوادم الحية أو فحص استقرار التشغيل الفعلي على بيئة الإنتاج تماشياً مع قاعدة الفحص المقيد المقروء فقط لضمان سلامة العمليات التجارية الجارية للعملاء (`PRODUCTION_STATUS_NOT_VERIFIED_IN_THIS_AUDIT`).
- **دورة حياة المهاجرات الهدامة للإنتاج**: لم تخضع سيناريوهات التراجع المباشر للإنتاج أو استعادة النسخ الاحتياطي للفحص اللحظي الفعلي.
- **تكامل واجهات المستخدم E2E**: تم تجميد اختبارات Playwright والواجهات لغياب بيئة اختبار معزولة وخاصة (`E2E_NOT_RUN_REQUIRES_ENV_SAFETY_REVIEW`).

---

## 5. الأدلة

تم الاستناد حصراً إلى الأدلة والقرائن البرمجية المستخلصة مباشرة من سطر أوامر الجلسة الحالية:
- **دليل التجميع وصحة الأنواع (EV-tsc)**: تشغيل `npx tsc --noEmit` بنجاح تام وبوجود **0 أخطاء برمجية** لكامل الـ 2200 ملف TypeScript. (`VERIFIED_BY_COMMAND_OUTPUT`)
- **دليل أخطاء Jest والتجميع (EV-jest)**: تعطل تجميع ملفات اختبارات مجلد `./tests` وظهور الخطأ `TS5011` لغياب الـ `rootDir` التكويني لملفات الاختبار. (`VERIFIED_BY_COMMAND_OUTPUT`)
- **دليل اختبارات عزل المستأجرين (EV-vitest)**: نجاح اختبارات تأمين عزل مسارات المستأجرين الـ 4 بالكامل تحت Vitest. (`VERIFIED_BY_CURRENT_TEST_RESULT`)
- **دليل إحصائيات الكود (EV-stats)**: وجود **6311** ملفاً متتبعاً في Git، تضم **887** مسار API و **526** صفحة واجهة مستخدم. (`VERIFIED_BY_COMMAND_OUTPUT`)
- **دليل التعريب ومفاتيح الترجمة (EV-i18n)**: توفر **3768** مفتاح ترجمة معرّب بالكامل داخل `src/locales/ar.json`. (`VERIFIED_BY_COMMAND_OUTPUT`)

---

## 6. النتائج (Master Baseline Table)

يوضح الجدول التالي تقييم الوضع الحالي وحالة كل مجال فني برمجياً مع الأدلة المبرهنة والفجوات المرصودة بوضوح وموضوعية:

| المجال الفني والتشغيلي | الحالة المعتمدة حالياً | الدليل البرمجي والبرهان المباشر | الفجوة المكتشفة برمجياً أو تنظيمياً | الأولوية |
| --------------------- | --------------------- | ------------------------------ | ----------------------------------- | -------- |
| **Quality & Testing** | `PARTIALLY_VERIFIED` | نجاح 1183 اختباراً في Jest و 1172 اختباراً في Vitest مع تجميد E2E. | تعثر اختبارات Jest لمجلد `tests` بسبب خطأ `TS5011` وتعارض ts-jest. | `P0_BLOCKER` |
| **Backend / API** | `PARTIAL` | مسار التوحيد يعتمد غلاف `withRoute` المركزي لحراسة الصلاحيات وعزل المستأجرين. | احتمال وجود مسارات فرعية مستحدثة تستدعي Prisma Client مباشرة. | `P1_HIGH` |
| **Database Schema** | `COMPLETE_BY_SCHEMA` | نماذج التوحيد والاستبعاد الـ 9 متواجدة بالكامل في مخطط قاعدة البيانات `prisma/schema.prisma`. | خلو بعض نماذج الجداول الكبيرة في المخطط من الفهارس المخصصة لـ `tenantId`. | `P2_MEDIUM` |
| **Frontend / UX** | `STRUCTURE_VERIFIED` | توفر 526 صفحة و 672 مكون واجهة ذكي مع توفير ملفات loading/error المعيارية. | جزم خلو الواجهات بالكامل من أي بليس هولدرز يتطلب فحوصاً وظيفية يدوية. | `P3_LOW` |
| **Tenant Isolation** | `PARTIAL_TEST_VERIFIED` | نجاح 4 اختبارات تأمينية لعزل وتأمين مسارات المستأجرين بنسبة 100% تحت Vitest. | `TENANT_ISOLATION_FULL_COVERAGE_NOT_PROVEN` لغياب فحص تغطية كامل المسارات. | `P1_HIGH` |
| **Financial Governance**| `COMPLETE_BY_CODE` | التزام محرك الاستبعاد `ConsolidationEliminationPostingService` ببوابات الموافقات وعزل الفترات. | يتطلب تشغيل دورة تجريبية معزولة للتأكد من موازين المراجعة الموحدة بالكامل. | `P1_HIGH` |
| **Saudi Compliance** | `PARTIAL` | هياكل توقيع XML وتوليد الـ CSR وحسابات GOSI والـ SIF متواجدة برمجياً. | الربط الفعلي للإنتاج لـ ZATCA غير مثبت دون وجود شهادات المستأجرين الحية. | `P1_HIGH` |
| **Performance** | `UNKNOWN` | غياب أي تقارير فحص أو أرقام حية مسجلة للأداء المالي تحت الضغط العالي. | عدم توفر Baseline رسمي لأداء العمليات المالية أو فحص N+1 للاستعلامات. | `P2_MEDIUM` |
| **DevOps & DR** | `PRODUCTION_NOT_VERIFIED`| وجود 15 ملف سير عمل CI/CD وملفات النشر الهيكلية مع تجميد الإنتاج. | غياب أدلة اختبار لسيناريوهات التراجع المباشر والـ backup restore الحية. | `P2_MEDIUM` |
| **Documentation & Legal**| `STRUCTURE_VERIFIED` | توفر خطة التعريب وملفات دليل الأعمال وذاكرة المشروع المبرهنة في الكود. | الحاجة إلى نقل التقارير التاريخية وتحديث أدلة التدريب ومحاكاة الموردين. | `P3_LOW` |

---

## 7. الفجوات

تم رصد الفجوات الهيكلية الحيوية التالية التي تعيق الارتقاء بالبنية البرمجية للنظام:
1. **تداخل مترجمات TypeScript وتجميع الاختبارات (GP-TEST-01)**: تعطل تشغيل اختبارات مجلد `./tests` تحت Jest لوجود تعارض تكويني للـ `rootDir` في `tsconfig.json` وغياب `tsconfig.test.json` مستقل. (`P0_BLOCKER`)
2. **خلط مكتبات الموك (GP-TEST-02)**: تعثر اختبارات الـ unit البرمجية الحساسة لتوظيف وظائف Jest Mocks (`jest.fn()`) داخل بيئة تشغيل Vitest القائمة. (`P1_HIGH`)
3. **غياب فحص التغطية الشاملة للاختبارات (GP-TEST-03)**: تعذر سحب وتوليد ملف التغطية التراكمي الموحد لتعارض بيئات التشغيل البرمجية. (`P2_MEDIUM`)
4. **حظر تفعيل الربط الإلكتروني للإنتاج (GP-ZATCA-01)**: غياب شهادات الإنتاج وخطوات Onboarding التفصيلية الحية لكل مستأجر على حدة في قواعد البيانات. (`P1_HIGH`)

---

## 8. المخاطر

يتعرض النظام البرمجي والتشغيلي للتهديدات والمخاطر الموثقة التالية:
1. **تداخل قيود أقفال الفترات وتواريخ عكس القيود (RK-TEST-01)**: خطر تعثر ترحيل معاملات الاستبعاد في حال حدوث محاولة ترحيل قيد العكس الفوري (Auto-Reversal) في تاريخ شهر مقفل أو منتهي محاسبياً. (الشدة: **حرجة**، قوة الدليل: **STRONG**)
2. **انحراف الأرصدة البينية بين التقديم والاعتماد (RK-TEST-02)**: خطر ترحيل أرصدة غير مطابقة في حال تغير موازين الحسابات البينية في النظام منذ لحظة تقديم طلب الاستبعاد إلى لحظة الترحيل الحقيقي. (الشدة: **حرجة**، قوة الدليل: **STRONG**)
3. **تجاوز حراس عزل المستأجرين في raw queries (RK-TEST-03)**: خطر حدوث تسريب أمني أفقياً في حال استدعاء استعلامات قواعد البيانات الخام مباشرة دون تصفية معرف المستأجر. (الشدة: **حرجة**، قوة الدليل: **MEDIUM**)
4. **كسر بناء الإنتاج عند تعديل إعدادات المترجم الرئيسي (RK-DEV-01)**: خطر فشل عملية البناء والـ build لنسخ سطح المكتب أو خوادم الويب في حال تعديل خيارات التجميع للمخطط العام. (الشدة: **حرجة**، قوة الدليل: **STRONG**)

---

## 9. تحديثات .ai-brain

تم تحديث وتأسيس نظام الذاكرة البرمجية المركزي `.ai-brain/` بالكامل كجزء من أعمال هذه المرحلة ليمثل المرجع الموحد والوحيد للحقيقة:
- **تحديث الفهرس [00-index.md](00-index.md)**: ربط وإضافة كافة مسارات ملفات قاعدة الذاكرة المستحدثة والمصححة وتنسيق الشجرة المعرفية.
- **إنشاء مستند الحالة الحالية [01-current-state.md](01-current-state.md)**: توثيق الحالة البرمجية الحقيقية بالأرقام المثبتة بوضوح وخلوها من التعميمات وتجميد الإنتاج.
- **إنشاء خطة الطريق العالمية [02-global-readiness-roadmap.md](02-global-readiness-roadmap.md)**: صياغة المراحل الـ 13 المتسلسلة لترقية النظام تقنياً وتنظيمياً مع عبارات الموافقات الصارمة.
- **إنشاء مستند الجودة والاختبارات [03-quality-and-testing.md](03-quality-and-testing.md)**: حصر تعارضات TypeScript وأسباب تعثر Jest و Vitest وآليات إصلاح بيئة الموكس والتغطية.
- **إنشاء سجل بوابات الحظر المالي [15-approval-gates.md](15-approval-gates.md)**: تنظيم عبارات الموافقة المخصصة ومنع القفز بين مراحل التطوير والتأهيل.
- **إنشاء وتوسيع سجل المخاطر [16-risk-register.md](16-risk-register.md)**: توثيق المخاطر الـ 20 المبرهنة والمحتملة مع تقييم قوة أدلتها.
- **إنشاء وتوسيع سجل الفجوات [17-gap-register.md](17-gap-register.md)**: حصر وتصنيف الفجوات الـ 4 الهيكلية بأولويات واقعية.
- **إنشاء سجل القرارات المعمارية [18-decision-log.md](18-decision-log.md)**: تدوين القرارات وتوصية فصل مسؤوليات Jest و Vitest وعزل tsconfig.
- **إنشاء سجل فهارس الأدلة [19-evidence-index.md](19-evidence-index.md)**: فهرسة وتوثيق كافة تقارير الجلسة القائمة وربطها التلقائي.
- **إنشاء ملف الخطوات القادمة [20-next-actions.md](20-next-actions.md)**: توضيح المعالجات البرمجية الفورية المتسلسلة المطلوبة.

---

## 10. التوصيات

يخلص التدقيق والتحليل المعماري الكلي إلى التوصية النهائية والمباشرة التالية:
> **نوصي باعتماد هذا التقرير وملفات الذاكرة البرمجية المستحدثة كقاعدة مرجعية مؤقتة (TEMPORARY_EVIDENCE_BASELINE)، والبدء الفوري لاحقاً في تنفيذ أول مرحلة عملية معلقة بموجب الموافقة الصريحة:**
> ```text
> GO_FOR_TEST_CONFIG_STABILIZATION_ONLY
> ```
> **السبب**: يمثل حل تعارض تجميع TypeScript للاختبارات (`TS5011`) وتوطيد إعدادات `tsconfig.test.json` البوابة الأولى والأساسية لتشغيل الـ 46 مشروع اختبار المتعطلة تحت Jest، وسحب أول تقرير تغطية تراكمي موحد وسليم للنظام، وهو ما يشكل الركيزة الأساسية قبل الارتقاء بالنظام إلى المعايير العالمية.

---

## 11. بوابة النجاح (Success Gate)

يُعتبر العمل في هذه المرحلة مكتملاً وناجحاً بالكامل عند تحقق الشروط التالية:
1. صياغة وإصدار تقرير الجاهزية الكلية `GLOBAL_READINESS_MASTER_BASELINE.md` بدقة وموضوعية.
2. تحديث وتأسيس ملفات قاعدة الذاكرة البرمجية الـ 10 التابعة لمجلد `.ai-brain/` وربطها بالكامل في الفهرس.
3. عدم إجراء أي تعديل أو إصلاح برمجي أو كتابة في قاعدة البيانات وتجميد الإنتاج بالكامل.

---

## 12. بوابة الإيقاف (Kill Switch)

يتم إيقاف المعالجة فوراً وإلغاء هذه المرحلة التخطيطية في الحالات التالية:
- أي محاولة لتغيير كود المحركات البرمجية أو المخططات أو قاعدة البيانات خلال هذه المرحلة التخطيطية.
- حدوث أي خرق أمني أو تسريب معلومات حقيقية للإنتاج أو إشارات للمفاتيح الحساسة.
- محاولة تشغيل أي اختبارات تتطلب ربطاً حقيقياً بخوادم هيئة الزكاة أو بنوك الإنتاج الحية.

---

## 13. هل يحتاج موافقة؟

**نعم، قطعاً**. تتطلب هذه المرحلة التخطيطية (المرحلة 0) اعتماد الإدارة والمسؤولين التقنيين رسمياً قبل الانتقال إلى أي خطوة عملية لاحقة لإصلاح أو تعديل البنية التحتية.

---

## 14. الخطوة التالية

بمجرد الحصول على اعتماد هذه الخطة التأسيسية، تكون الخطوة الفورية القادمة هي البدء في معالجة تعارضات TypeScript وتثبيت تجميع الاختبارات تحت إشراف وتوجيه عبارة الموافقة:
`GO_FOR_TEST_CONFIG_STABILIZATION_ONLY`

---

## Audit Safety Notes

- لم يتم تعديل أي شيء خارج نطاق الموافقة.
- لم يتم إنشاء commit إلا إذا كانت المرحلة تسمح بذلك.
- لم يتم push إلا إذا كانت المرحلة تسمح بذلك.
- لم يتم deploy إلا إذا كانت المرحلة تسمح بذلك.
- لم يتم تشغيل migration إلا إذا كانت المرحلة تسمح بذلك.
- لم يتم لمس production إلا إذا كانت المرحلة تسمح بذلك.
- تم تحديث .ai-brain حسب نطاق المرحلة.
