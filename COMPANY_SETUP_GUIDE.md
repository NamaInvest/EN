# دليل إطلاق وإنشاء فرع شركة جديدة (Multi-Tenant)

هذا الدليل أُعد خصيصاً لتيسير عملية استنساخ وتشغيل نظام مبيعات جديد لأي شركة قادمة (مثل n2, n3, إلخ) على سيرفر aaPanel في أقل من 5 دقائق وبدون أخطاء.

---

### الخطوة 1: تجهيز السيرفر وحفظ الكود
1. في لوحة **aaPanel**، اذهب إلى `Website`.
2. اضغط **Add Site** وأدخل الدومين الجديد (مثال: `n2.namainvist.com`).
3. عند الإنشاء، اختر خيار إنشاء قاعدة بيانات **Database** جديدة (احفظ اسم المستخدم وكلمة السر التي سينشئها لك).
4. بعد الإنشاء، انسخ ملفات الكود المضغوطة `deploy_n1.zip` وانقلها إلى المسار الجديد الخاص بالشركة الثانية `www/wwwroot/n2...` وفك الضغط هناك.

### الخطوة 2: إنشاء ملف الوصلة السحرية (.env)
داخل مجلد الشركة الجديد في السيرفر، اصنع ملفاً نصياً صغيراً جداً وقم بتسميته `.env` واكتب فيه هذا الكود بعد تعديل بيانات القاعدة:
```env
DATABASE_URL="postgresql://اسم_قاعدة_البيانات:كلمة_السر_الخاصة_بها@127.0.0.1:5432/اسم_قاعدة_البيانات"
GEMINI_API_KEY=""
TELEGRAM_BOT_TOKEN=""
```

### الخطوة 3: أمر التثبيت والإقلاع بضغطة زر
افتح نافذة الأوامر (Terminal) المدمجة باللوحة، وتأكد أنك داخل مجلد الشركة الجديدة.. ثم اكتب الأوامر التالية بالترتيب:

```bash
# تثبيت الحزم والمكتبات
npm install

# بناء وهيكلة قاعدة البيانات + إنشاء حساب المدير وإعدادات الضرائب الآلية (بضغطة واحدة)
npm run db:setup

# ضغط وتجهيز الموقع كنسخة إنتاج سريعة
npm run build
```

### الخطوة 4: تشغيل الموقع بشكل دائم
1. من يسار لوحة aaPanel، اذهب إلى `Node Project`.
2. اختر زر **Add Node project**.
3. **المسار (Path)**: اختر مجلد الشركة الجديدة `www/wwwroot/n2...`.
4. **الاسم (Name)**: اكتب اسماً قصيراً بدون نقاط (مثال: `n2_company`).
5. **طريقة الإقلاع (Run opt)**: اختر `[Custom command]` أو أمر مخصص واكتب فيه هذا الأمر ليعمل على بوابة منفصلة (غير الرقم للبوابة الجديدة التي تريدها):
   `npm run start -- -p 3002`
6. **المنفذ (Port)**: اكتب الرقم الجديد (مثال: `3002`).
7. **المستخدم (User)**: تأكد أنه `root`.
8. **الدومين (Domain)**: اكتب الدومين في المربع الكبير بالأسفل (مثال: `n2.namainvist.com`).
9. اضغط **Confirm** وسيصبح نظامه باللون الأخضر ويقلع!

---

### الخطوة 5 وأخيراً: توجيه زوار الإنترنت من Namecheap
في إدارة Namecheap الخاصة بالدومين الرئيسي:
- أضف **A Record** جديد.
- الـ Host: `n2`
- الـ Value: عنوان الآيبي الخاص بسيرفرك `46.4.188.170`.
- احفظ الريكورد وانتظر دقيقة.

مبروك! موقع الشركة الجديدة يعمل 100% بالمدير الافتراضي (`admin` / `admin`).
