# 16 - سجل وتصنيفات المخاطر المعمارية والتشغيلية (Risk Register)

> **آخر تحديث:** 2026-06-02 | **سجل المخاطر النشطة وإجراءات التخفيف** | **ثابت معتمد**

---

## 📌 فلسفة إدارة المخاطر في Nama Invest
يتعامل مشروع **Nama Invest ERP** مع معاملات مالية وتشغيلية بالغة الأهمية. لذلك، يتم توثيق وتحليل كافة المخاطر المحتملة والنشطة بدقة تامة وتحديد مستويات شدتها وقوة الأدلة المبرهنة عليها من الكود الفعلي لتطبيق معالجة وقائية صارمة ومنع تعطل الأعمال.

---

## 📊 سجل المخاطر الـ 20 المبرهنة والنشطة (Risk Register)

يتم حصر وتصنيف المخاطر الأمنية، المالية، والتشغيلية وفقاً للمصفوفة التفصيلية التالية:

| كود الخطر | وصف وتفاصيل الخطر | التصنيف والنوع | مستوى الشدة | قوة الدليل الفعلي | الإجراء الوقائي المقترح وتخفيف الخطر | حالة الخطر |
| --------- | ----------------- | -------------- | ----------- | ----------------- | ------------------------------------- | ---------- |
| **RK-FIN-01**| تداخل قيود أقفال الفترات وتواريخ عكس قيود استبعاد التوحيد مطلع الشهر في فترة محاسبية منتهية أو مقفلة. | مالي محاسبي | **CRITICAL**| `STRONG` (مبرهن بكود المحرك المالي) | منع ترحيل قيود العكس التلقائي في حال كانت الفترة المستهدفة مقفلة محاسبياً وتوجيه القيد لأقرب فترة مفتوحة مع إشعار مالي. | `ACTIVE` |
| **RK-FIN-02**| انحراف أرصدة المعاملات البينية بين لحظة تقديم طلب الاستبعاد والاعتماد النهائي في حال حدوث عمليات ترحيل جديدة. | مالي محاسبي | **CRITICAL**| `STRONG` (مبرهن بكود الاستبعاد) | تجميد لقطة الأرصدة البينية (Snapshot) عند التقديم وإعادة الفحص التلقائي لمطابقة الأرصدة قبل توليد قيد الترحيل مباشرة لمنع التفاوت. | `ACTIVE` |
| **RK-SEC-01**| حدوث تسريب أمني أو وصول غير مصرح عبر استعلامات قواعد البيانات الخام (Raw Queries) دون حقن حراس المستأجرين. | أمني سيبراني | **CRITICAL**| `MEDIUM` (احتمال وجود raw SQL في موديولات قديمة) | منع استعلامات raw SQL تماماً دون تدقيق يدوي مسبق، وإلزام المطورين بالمرور عبر مساعد `getPrisma(req)`. | `ACTIVE` |
| **RK-DEV-01**| فشل عملية البناء والـ build لتطبيقات سطح المكتب أو خوادم الويب في حال تعديل إعدادات المترجم الرئيسي للمخطط. | عملياتي تشغيلي | **CRITICAL**| `STRONG` (فحص ملفات التكوين والاعتماديات) | عزل كامل لإعدادات تجميع الاختبارات في ملف `tsconfig.test.json` مستقل لتجنب تعارض تجميع محركات الإنتاج. | `ACTIVE` |
| **RK-TST-01**| خلط وتداخل أدوار واختبارات Jest و Vitest مما يتسبب في توقف الاختبارات الحساسة للمحركات لتعارض دوال الموك. | جودة برمجية | **HIGH** | `STRONG` (تعطل 69 اختباراً في Vitest) | فصل تام للمسؤوليات، إسناد اختبارات الـ Unit لـ Vitest وتكامل الـ API لـ Jest، وتوطيد دوال `vi.fn()` محلياً. | `ACTIVE` |
| **RK-TST-02**| عدم دقة وسلامة اختبارات واجهة المستخدم وتكامل الـ API لغياب فحص التغطية الموحد وتقرير التغطية التراكمي. | جودة برمجية | **HIGH** | `STRONG` (مقياس التغطية غير مولّد) | معالجة إعدادات المترجم وتوليد ملف التغطية `coverage-summary.json` وربطه بالـ CI كبوابة جودة إلزامية. | `ACTIVE` |
| **RK-ZAT-01**| فشل إرسال وتوقيع فواتير المبيعات الإلكترونية لـ ZATCA في حال انقطاع الإنترنت أو بطء خوادم الاستجابة الحكومية. | امتثال ضريبي | **HIGH** | `STRONG` (كود التكامل والربط مع ZATCA) | تفعيل طوابير المعالجة الخلفية وإلزام نقاط البيع بتوقيع الفاتورة محلياً وحفظها في SQLite وإعادة الإرسال تلقائياً فور الاتصال. | `ACTIVE` |
| **RK-SEC-02**| تسريب كلمات مرور أو مفاتيح سرية وشهادات ربط ZATCA حية في ملفات المستودع وتاريخ Git التراكمي. | أمني سيبراني | **HIGH** | `MEDIUM` (يتطلب فحص gitleaks) | تشغيل فحوص Trufflehog و Gitleaks دورياً، ونقل كافة المفاتيح لملف البيئة `.env` المحمي والمشفر. | `ACTIVE` |
| **RK-SEC-03**| إمكانية تعديل أو تزييف سجلات العمليات والأخطاء الحساسة في حال عدم تأمين جدول سجل التدقيق وقفل سجل الحركات. | أمني سيبراني | **HIGH** | `STRONG` (جدول سجلات الحركات) | حظر عمليات التعديل والحذف برمجياً وعلى مستوى قاعدة البيانات لجدول سجلات التدقيق وضبط القفل التام للحماية. | `ACTIVE` |
| **RK-PER-01**| تباطؤ استرجاع كشوف الحسابات والتقارير المالية المليئة بالمعاملات في حال خلو قاعدة البيانات من الفهارس المثلى. | أداء وتوسع | **HIGH** | `MEDIUM` (يتطلب فحص indexes) | فرض فهارس مخصصة ومركبة لكافة الحقول المستخدمة للتصفية والربط المالي مثل `tenantId` و `createdAt` و `posted`. | `ACTIVE` |
| **RK-OPS-01**| انقطاع أو تعطل الخدمة السحابية للعملاء أثناء نشر تحديثات أو مهاجرات قواعد البيانات الحساسة على خوادم الإنتاج. | عملياتي تشغيلي | **HIGH** | `STRONG` (دليل ملفات النشر الحية) | فرض بيئة Staging مطابقة للإنتاج لاختبار المهاجرات والتراجع الآمن بالكامل قبل النشر الحقيقي، واستخدام مفاتيح الميزات. | `ACTIVE` |
| **RK-HR-01** | رفض البنوك لملف حماية الأجور (SIF) وتأخر رواتب الموظفين لعدم مطابقة هيكل الملف لمتطلبات المصارف والوزارة. | امتثال مالي | **HIGH** | `STRONG` (كود توليد ملف الـ SIF) | تحديث معادلات ومخططات احتساب الرواتب والبدلات بدقة وتفعيل مدقق آلي لهيكل ملف SIF قبل تصديره للتحقق التام. | `ACTIVE` |
| **RK-PDPL-01**| خرق نظام حماية البيانات الشخصية السعودي (PDPL) في حال تسريب بيانات المستخدمين أو عدم تفعيل طلبات الحذف والتصحيح. | امتثال قانوني | **HIGH** | `MEDIUM` (يتطلب مراجعة تدفق البيانات) | بناء مسار عمل آلي لمعالجة طلبات أصحاب البيانات (DSR) وتأمين سجل الموافقات المشفر وتحديد فترات أمان صارمة للملفات. | `ACTIVE` |
| **RK-UX-01** | تشتت مستخدمي النظام وقلقهم من توقف المعالجة المحاسبية للعمليات الكبيرة في حال خلو الواجهات من مؤشرات تحميل بصرية. | تجربة مستخدم | **MEDIUM**| `STRONG` (فحص مكونات الواجهة المدمجة) | تزويد شاشات الإدخال والتقارير بمؤشرات تحميل بصرية عصرية (Skeletons) وتوضيح خطوات الحل في شاشات الأخطاء. | `ACTIVE` |
| **RK-UX-02** | تداخل وتشويه نصوص واجهة المستخدم العربية RTL لغياب قيود تجاوز الأسطر وتدفق العناصر بشكل صحيح في الشاشات. | تجربة مستخدم | **MEDIUM**| `STRONG` (التعريب وتوافق RTL) | استخدام خطوط وتصميم RTL مرن ومتناسق بالكامل والتأكد من ملاءمة النصوص للمربعات والشاشات بمختلف مقاساتها. | `ACTIVE` |
| **RK-LEG-01**| وقوع المسؤولية القانونية على المنصة في حال خلو اتفاقيات مستوى الخدمة (SLA) وشروط الاستخدام من تفاصيل معالجة البيانات. | امتثال قانوني | **MEDIUM**| `STRONG` (الباقة التعاقدية للمشروع) | إعداد واعتماد ToS, Privacy Policy, DPA واضحة وصياغة حدود المسؤولية ومستويات التوافر السحابية رسمياً وعقدياً. | `ACTIVE` |
| **RK-MKT-01**| صعوبة بيع وإقناع كبار المستثمرين والمجموعات بمتانة النظام في حال عدم مقارنة الميزات تنافسياً مع SAP و NetSuite. | تسويقي تجاري | **MEDIUM**| `STRONG` (وثيقة المقارنة والتميز) | إعداد ملف فني متكامل يبرهن سرعة وجودة وقوة ميزات Nama Invest ERP الضريبية والمالية السحابية وتفوقها بالدليل. | `ACTIVE` |
| **RK-PER-02**| حدوث تباطؤ واستهلاك كامل للذاكرة والمعالج في خادم قواعد البيانات لعدم تفعيل طبقة تخزين مؤقت مناسبة للبيانات الثابتة. | أداء وتوسع | **MEDIUM**| `MEDIUM` (يتطلب مراجعة Redis) | تفعيل Redis Caching معزول لكل مستأجر لحفظ شجرة الحسابات والتهيئة لتقليل الاستعلامات وحماية أداء الخوادم. | `ACTIVE` |
| **RK-DEV-02**| تعثر المطورين والشركاء في دمج إضافات جديدة للأنظمة لعدم وضوح بيئة التوطين وهندسة الربط عبر الـ API. | عملياتي تشغيلي | **MEDIUM**| `STRONG` (كود التهيئة والربط البرمجي) | بناء شراكات التجميع (Partner Ecosystem) وصياغة وثيقة الـ SDK والـ APIs الآمنة المعزولة للمطورين دون خطر على الكود. | `ACTIVE` |
| **RK-OPS-02**| فقدان البيانات الحساسة أو فترات محاسبية كاملة للعملاء لعدم انتظام النسخ الاحتياطي أو تعطل خادم التخزين الرئيسي. | عملياتي تشغيلي | **CRITICAL**| `STRONG` (دليل النسخ واستعادة الكوارث) | انتظام جدول النسخ الاحتياطي اليومي المشفر ونقله تلقائياً لخوادم خارج الموقع (Off-site) وتأكيد صلاحية النسخ دورياً. | `ACTIVE` |
| **RK-MCP-01**| قيام خادم MCP أو وكلاء الذكاء الاصطناعي بقراءة المفاتيح البيئية والرموز السرية الحساسة من ملف `.env` وعرضها بالـ logs. | أمني سيبراني | **HIGH** | `MEDIUM` (يتطلب مراجعة مخرجات الفحوص) | فرض حظر فيزيائي مشدد بالـ Deny list يمنع قراءة أو سحب ملف `.env` وشهادات الاتصال نهائياً من الـ MCP. | `ACTIVE` |
| **RK-MCP-02**| قيام خادم MCP أو وكيل الذكاء بتعديل أو إضافة كود تشغيلي للمحركات البرمجية أو المخططات دون مراجعة معمارية مسبقة. | جودة معمارية | **HIGH** | `LOW` (يتطلب مراجعة الصلاحيات) | حظر صلاحيات كتابة الكود كلياً لخوادم الـ MCP وقصرها حصرياً على كتابة التقارير وتحديث الذاكرة `.ai-brain/`. | `ACTIVE` |
| **RK-MCP-03**| تشغيل أوامر هدامة لقواعد البيانات (مثل drop table) أو تغيير البنية بشكل قسري على بيئة الإنتاج أو staging الحية. | عمليات ماليّة | **CRITICAL**| `LOW` (يتطلب ضبط الأوامر) | حظر أوامر rm, del, drop, db push بالـ Deny list المباشر وعزل الاتصالات مع قواعد Staging وصامتاً بالكامل. | `ACTIVE` |
| **RK-MCP-04**| انحراف ذاكرة المشروع وكتابة وتحديث `.ai-brain` بمعلومات إنشائية وتوقعات غير مدعومة بقرينة حية أو دليل برمجي. | جودة برمجية | **MEDIUM**| `HIGH` (يتطلب حوكمة السكربتات) | فرض وتفعيل تشغيل سكربت فلترة وتدقيق الأدلة `validate-evidence-tags.ts` تلقائياً لمنع أي كتابة إنشائية غير موثقة. | `ACTIVE` |
| **RK-SEC-04**| وجود 56 ثغرة برمجية في حزم التبعيات البرمجية تهدد باختراقات أمنية أو تسريبات لبيانات المستأجرين الحساسة. | أمني سيبراني | **HIGH** | `STRONG` (نتائج npm audit التفصيلية) | تشغيل ترقيات مخصصة وحزمOverrides لـ xmldom و serialize-javascript و path-to-regexp دون كسر توافقية الواجهات. | `RESOLVED` |
| **RK-DEV-03**| كسر توافقية التجميع (TypeScript) أو تعطل بناء المشروع للإنتاج (Next.js build) نتيجة ترقية قسرية للتبكيات. | عملياتي تشغيلي | **HIGH** | `STRONG` (محاذير package.json) | فرض تشغيل فحوصات `npm run typecheck` و `npm run lint` وبيئات staging معزولة تماماً قبل دمج أي تحديث حزم. | `MITIGATED` |
| **RK-TST-03**| فشل اختبار عزل المستأجرين US-SECURITY-002 في Vitest نتيجة تجاوزه مهلة الانتظار 5000ms، مما يمثل خطراً محتملاً لسلامة وثبات محرك العزل أو بطء خادم الاختبارات. | جودة برمجية | **HIGH** | `STRONG` (تعطل وفشل بوابة Vitest) | تخصيص مرحلة تحليل فورية `GO_FOR_TEST_FAILURE_ANALYSIS_ONLY` لفحص وضبط المهلة الزمنية وتوطيد استقرار محرك عزل المستأجرين. | `ACTIVE` |


