export class RoutingService {
  async getRoutingForProduct(itemId: string) {
    // Stub: Operations sequence per item
    // Setup time + Run time
    // Resource (machine + labor)
    return { operations: [] };
  }
}
