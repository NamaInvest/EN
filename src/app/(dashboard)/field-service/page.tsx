'use client';
import { useState, useEffect } from 'react';
import { Compass, Plus, MapPin } from 'lucide-react';
import { useToast } from '@/components/Toast';
import { useTranslation } from '@/lib/i18n';
export default function FieldServicePage() {
  const { error: te, success: ts } = useToast();
  const { lang } = useTranslation();
  const _t = (ar: string, en: string) => lang === 'ar' ? ar : en;
  const [orders, setOrders] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);
  const [showModal, setShowModal] = useState(false);
  const [form, setForm] = useState<any>({customerName:'',address:'',technicianName:'',scheduledDate:'',priority:'MEDIUM',serviceType:'REPAIR',description:''});
  useEffect(() => { load(); }, []);
  const load = async () => { setLoading(true); try { const r = await fetch('/api/field-service/orders',{headers:{Authorization:`Bearer ${localStorage.getItem('token')}`}}); if(r.ok) setOrders(await r.json()); } catch{} finally { setLoading(false); } };
  const save = async (e:React.FormEvent) => { e.preventDefault(); try { await fetch('/api/field-service/orders',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${localStorage.getItem('token')}`},body:JSON.stringify(form)}); ts(_t('تم','Done')); setShowModal(false); load(); } catch(e:any){te(e?.message);} };
  const pc:any = {LOW:'#22C55E',MEDIUM:'#EAB308',HIGH:'#F97316',CRITICAL:'#EF4444'};
  const pl:any = {LOW:_t('منخفضة','Low'),MEDIUM:_t('متوسطة','Medium'),HIGH:_t('عالية','High'),CRITICAL:_t('حرجة','Critical')};
  const sc:any = {SCHEDULED:'#3B82F6',DISPATCHED:'#8B5CF6',IN_PROGRESS:'#EAB308',COMPLETED:'#22C55E',CANCELLED:'#94A3B8'};
  const stl:any = {REPAIR:_t('إصلاح','Repair'),INSTALL:_t('تركيب','Install'),INSPECTION:_t('فحص','Inspection'),MAINTENANCE:_t('صيانة','Maintenance')};
  return (<div style={{padding:'24px'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:'24px',flexWrap:'wrap',gap:'16px'}}><div><h1 style={{fontSize:'24px',fontWeight:'bold',display:'flex',alignItems:'center',gap:'10px'}}><Compass size={28} color="var(--primary)"/> {_t('الخدمة الميدانية','Field Service')}</h1><p style={{color:'var(--text-muted)',marginTop:'4px',fontSize:'14px'}}>{_t('إدارة الفنيين والزيارات','Technician & Visit Management')}</p></div><button className="btn btn-primary" onClick={()=>{setForm({customerName:'',address:'',technicianName:'',scheduledDate:'',priority:'MEDIUM',serviceType:'REPAIR',description:''});setShowModal(true);}}><Plus size={20}/> {_t('أمر خدمة','Service Order')}</button></div>
  <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(160px,1fr))',gap:'16px',marginBottom:'24px'}}>{[{l:_t('مجدولة','Scheduled'),v:orders.filter(o=>o.status==='SCHEDULED').length,c:'#3B82F6'},{l:_t('جارية','In Progress'),v:orders.filter(o=>o.status==='IN_PROGRESS').length,c:'#EAB308'},{l:_t('مكتملة','Completed'),v:orders.filter(o=>o.status==='COMPLETED').length,c:'#22C55E'},{l:_t('التكلفة','Cost'),v:orders.reduce((a:number,o:any)=>a+(o.cost||0),0).toLocaleString()+` ${_t('ر.س','SAR')}`,c:'#6366F1'}].map((s,i)=>(<div key={i} className="card" style={{padding:'16px',borderTop:`3px solid ${s.c}`}}><div style={{fontSize:'12px',color:'var(--text-muted)'}}>{s.l}</div><div style={{fontSize:'24px',fontWeight:'800',color:s.c,marginTop:'4px'}}>{s.v}</div></div>))}</div>
  {loading?<div style={{textAlign:'center',padding:'40px'}}>{_t('جاري التحميل...','Loading...')}</div>:(<div className="card" style={{overflow:'auto'}}><table className="table"><thead><tr><th>{_t('الرقم','Order #')}</th><th>{_t('العميل','Customer')}</th><th>{_t('العنوان','Address')}</th><th>{_t('الفني','Technician')}</th><th>{_t('النوع','Type')}</th><th>{_t('الأولوية','Priority')}</th><th>{_t('الموعد','Date')}</th><th>{_t('الحالة','Status')}</th></tr></thead><tbody>{orders.map(o=>(<tr key={o.id}><td style={{fontWeight:'600',fontFamily:'monospace'}}>{o.orderNo}</td><td>{o.customerName||'-'}</td><td style={{fontSize:'12px'}}><MapPin size={12} style={{display:'inline',marginLeft:'2px'}}/>{o.address||'-'}</td><td>{o.technicianName||'-'}</td><td>{stl[o.serviceType]||o.serviceType}</td><td><span style={{padding:'2px 8px',borderRadius:'8px',fontSize:'11px',fontWeight:'600',background:(pc[o.priority]||'#94A3B8')+'20',color:pc[o.priority]}}>{pl[o.priority]||o.priority}</span></td><td style={{fontSize:'12px'}}>{o.scheduledDate?new Date(o.scheduledDate).toLocaleDateString(lang==='ar'?'ar-SA':'en-US'):'-'}</td><td><span style={{padding:'2px 8px',borderRadius:'8px',fontSize:'11px',fontWeight:'600',background:(sc[o.status]||'#94A3B8')+'20',color:sc[o.status]}}>{o.status}</span></td></tr>))}{orders.length===0&&<tr><td colSpan={8} style={{textAlign:'center',padding:'30px',color:'var(--text-muted)'}}>{_t('لا توجد أوامر','No orders')}</td></tr>}</tbody></table></div>)}
  {showModal&&(<div className="modal-overlay"><div className="modal-content" style={{maxWidth:'550px'}}><div className="modal-header"><h2>{_t('أمر خدمة ميدانية','Field Service Order')}</h2><button className="btn btn-ghost" onClick={()=>setShowModal(false)}>✕</button></div><div className="modal-body"><form onSubmit={save}><div className="grid-2"><div className="input-group"><label className="input-label">{_t('العميل','Customer')}*</label><input className="input" required value={form.customerName} onChange={e=>setForm({...form,customerName:e.target.value})}/></div><div className="input-group"><label className="input-label">{_t('الفني','Technician')}</label><input className="input" value={form.technicianName} onChange={e=>setForm({...form,technicianName:e.target.value})}/></div><div className="input-group" style={{gridColumn:'1/-1'}}><label className="input-label">{_t('العنوان','Address')}</label><input className="input" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></div><div className="input-group"><label className="input-label">{_t('النوع','Type')}</label><select className="input" value={form.serviceType} onChange={e=>setForm({...form,serviceType:e.target.value})}><option value="REPAIR">{_t('إصلاح','Repair')}</option><option value="INSTALL">{_t('تركيب','Install')}</option><option value="INSPECTION">{_t('فحص','Inspection')}</option><option value="MAINTENANCE">{_t('صيانة','Maintenance')}</option></select></div><div className="input-group"><label className="input-label">{_t('الأولوية','Priority')}</label><select className="input" value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}><option value="LOW">{_t('منخفضة','Low')}</option><option value="MEDIUM">{_t('متوسطة','Medium')}</option><option value="HIGH">{_t('عالية','High')}</option><option value="CRITICAL">{_t('حرجة','Critical')}</option></select></div><div className="input-group"><label className="input-label">{_t('الموعد','Date')}</label><input className="input" type="datetime-local" value={form.scheduledDate} onChange={e=>setForm({...form,scheduledDate:e.target.value})}/></div></div><div style={{display:'flex',justifyContent:'flex-end',gap:'12px',marginTop:'20px'}}><button type="button" className="btn btn-ghost" onClick={()=>setShowModal(false)}>{_t('إلغاء','Cancel')}</button><button type="submit" className="btn btn-primary">{_t('حفظ','Save')}</button></div></form></div></div></div>)}</div>);
}
