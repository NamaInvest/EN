'use client';
import { useState, useEffect } from 'react';
import { Globe, Plus, Users, Mail } from 'lucide-react';
import { useToast } from '@/components/Toast';
import { useTranslation } from '@/lib/i18n';
export default function PortalPage() {
  const { error: te, success: ts } = useToast();
  const { lang } = useTranslation();
  const _t = (ar: string, en: string) => lang === 'ar' ? ar : en;
  const [users, setUsers] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);
  const [showModal, setShowModal] = useState(false);
  const [form, setForm] = useState<any>({name:'',email:'',password:'123456'});
  useEffect(() => { load(); }, []);
  const load = async () => { setLoading(true); try { const r = await fetch('/api/portal/users',{headers:{Authorization:`Bearer ${localStorage.getItem('token')}`}}); if(r.ok) setUsers(await r.json()); } catch{} finally { setLoading(false); } };
  const save = async (e:React.FormEvent) => { e.preventDefault(); try { await fetch('/api/portal/users',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${localStorage.getItem('token')}`},body:JSON.stringify(form)}); ts(_t('تم','Done')); setShowModal(false); load(); } catch(e:any){te(e?.message);} };
  return (<div style={{padding:'24px'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:'24px',flexWrap:'wrap',gap:'16px'}}><div><h1 style={{fontSize:'24px',fontWeight:'bold',display:'flex',alignItems:'center',gap:'10px'}}><Globe size={28} color="var(--primary)"/> {_t('بوابة العملاء','Customer Portal')}</h1><p style={{color:'var(--text-muted)',marginTop:'4px',fontSize:'14px'}}>{_t('إدارة مستخدمي البوابة','Manage portal users')}</p></div><button className="btn btn-primary" onClick={()=>{setForm({name:'',email:'',password:'123456'});setShowModal(true);}}><Plus size={20}/> {_t('مستخدم جديد','New User')}</button></div>
  <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(180px,1fr))',gap:'16px',marginBottom:'24px'}}>{[{l:_t('الإجمالي','Total'),v:users.length,c:'#6366F1'},{l:_t('نشط','Active'),v:users.filter(u=>u.status==='ACTIVE').length,c:'#22C55E'},{l:_t('غير نشط','Inactive'),v:users.filter(u=>u.status!=='ACTIVE').length,c:'#94A3B8'}].map((s,i)=>(<div key={i} className="card" style={{padding:'16px',borderTop:`3px solid ${s.c}`}}><div style={{fontSize:'12px',color:'var(--text-muted)'}}>{s.l}</div><div style={{fontSize:'28px',fontWeight:'800',color:s.c,marginTop:'4px'}}>{s.v}</div></div>))}</div>
  {loading?<div style={{textAlign:'center',padding:'40px'}}>{_t('جاري التحميل...','Loading...')}</div>:(<div className="card" style={{overflow:'auto'}}><table className="table"><thead><tr><th>{_t('الاسم','Name')}</th><th>{_t('البريد','Email')}</th><th>{_t('آخر دخول','Last Login')}</th><th>{_t('الحالة','Status')}</th></tr></thead><tbody>{users.map(u=>(<tr key={u.id}><td style={{fontWeight:'600'}}>{u.name}</td><td style={{fontSize:'13px',direction:'ltr'}}><Mail size={12} style={{display:'inline',marginLeft:'4px'}}/>{u.email}</td><td style={{fontSize:'12px'}}>{u.lastLogin?new Date(u.lastLogin).toLocaleString(lang==='ar'?'ar-SA':'en-US'):'-'}</td><td><span style={{padding:'2px 8px',borderRadius:'8px',fontSize:'11px',fontWeight:'600',background:u.status==='ACTIVE'?'#22C55E20':'#94A3B820',color:u.status==='ACTIVE'?'#22C55E':'#94A3B8'}}>{u.status==='ACTIVE'?_t('نشط','Active'):_t('غير نشط','Inactive')}</span></td></tr>))}{users.length===0&&<tr><td colSpan={4} style={{textAlign:'center',padding:'30px',color:'var(--text-muted)'}}>{_t('لا يوجد مستخدمون','No users')}</td></tr>}</tbody></table></div>)}
  {showModal&&(<div className="modal-overlay"><div className="modal-content" style={{maxWidth:'450px'}}><div className="modal-header"><h2>{_t('مستخدم جديد','New User')}</h2><button className="btn btn-ghost" onClick={()=>setShowModal(false)}>✕</button></div><div className="modal-body"><form onSubmit={save}><div className="input-group"><label className="input-label">{_t('الاسم','Name')}*</label><input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="input-group"><label className="input-label">{_t('البريد','Email')}*</label><input className="input" type="email" dir="ltr" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="input-group"><label className="input-label">{_t('كلمة المرور','Password')}</label><input className="input" dir="ltr" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><div style={{display:'flex',justifyContent:'flex-end',gap:'12px',marginTop:'20px'}}><button type="button" className="btn btn-ghost" onClick={()=>setShowModal(false)}>{_t('إلغاء','Cancel')}</button><button type="submit" className="btn btn-primary">{_t('إنشاء','Create')}</button></div></form></div></div></div>)}</div>);
}
