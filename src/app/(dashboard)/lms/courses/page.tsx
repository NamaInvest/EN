'use client';
import { useState, useEffect } from 'react';
import { useTranslation } from '@/lib/i18n';
import { GraduationCap, Plus, Users, BookOpen } from 'lucide-react';
import { useToast } from '@/components/Toast';
export default function LMSPage() {
  const { lang } = useTranslation();
  const _t = (ar: string, en: string) => lang === 'ar' ? ar : en;
  const { error: te, success: ts } = useToast();
  const [courses, setCourses] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);
  const [showModal, setShowModal] = useState(false);
  const [form, setForm] = useState<any>({title:'',description:'',category:'',instructor:'',duration:0,level:'BEGINNER'});
  useEffect(() => { load(); }, []);
  const load = async () => { setLoading(true); try { const r = await fetch('/api/lms/courses',{headers:{Authorization:`Bearer ${localStorage.getItem('token')}`}}); if(r.ok) setCourses(await r.json()); } catch{} finally { setLoading(false); } };
  const save = async (e:React.FormEvent) => { e.preventDefault(); try { await fetch('/api/lms/courses',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${localStorage.getItem('token')}`},body:JSON.stringify(form)}); ts('تم'); setShowModal(false); load(); } catch(e:any){te(e?.message);} };
  const lc:any = {BEGINNER:'#22C55E',INTERMEDIATE:'#EAB308',ADVANCED:'#EF4444'};
  const ll:any = {BEGINNER:'مبتدئ',INTERMEDIATE:'متوسط',ADVANCED:'متقدم'};
  return (<div style={{padding:'24px'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:'24px',flexWrap:'wrap',gap:'16px'}}><div><h1 style={{fontSize:'24px',fontWeight:'bold',display:'flex',alignItems:'center',gap:'10px'}}><GraduationCap size={28} color="var(--primary)" /> التعلم الإلكتروني (LMS)</h1><p style={{color:'var(--text-muted)',marginTop:'4px',fontSize:'14px'}}>الدورات التدريبية والتعلم</p></div><button className="btn btn-primary" onClick={()=>{setForm({title:'',description:'',category:'',instructor:'',duration:0,level:'BEGINNER'});setShowModal(true);}}><Plus size={20}/> دورة جديدة</button></div>
  <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(180px,1fr))',gap:'16px',marginBottom:'24px'}}>{[{l:'إجمالي الدورات',v:courses.length,c:'#6366F1'},{l:'المحتوى',v:courses.reduce((a:number,c:any)=>a+(c._count?.modules||0),0),c:'#3B82F6'},{l:'المسجلين',v:courses.reduce((a:number,c:any)=>a+(c._count?.enrollments||0),0),c:'#22C55E'}].map((s,i)=>(<div key={i} className="card" style={{padding:'16px',borderTop:`3px solid ${s.c}`}}><div style={{fontSize:'12px',color:'var(--text-muted)'}}>{s.l}</div><div style={{fontSize:'28px',fontWeight:'800',color:s.c,marginTop:'4px'}}>{s.v}</div></div>))}</div>
  {loading?<div style={{textAlign:'center',padding:'40px'}}>جاري التحميل...</div>:(<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(300px,1fr))',gap:'16px'}}>{courses.map(c=>(<div key={c.id} className="card" style={{padding:'20px'}}><div style={{display:'flex',justifyContent:'space-between',marginBottom:'10px'}}><span style={{padding:'2px 8px',borderRadius:'8px',fontSize:'11px',fontWeight:'600',background:(lc[c.level]||'#94A3B8')+'20',color:lc[c.level]}}>{ll[c.level]||c.level}</span>{c.published&&<span style={{fontSize:'11px',color:'#22C55E',fontWeight:'600'}}>✓ منشور</span>}</div><h3 style={{fontSize:'18px',fontWeight:'700',marginBottom:'8px'}}>{c.title}</h3>{c.description&&<p style={{fontSize:'13px',color:'var(--text-muted)',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{c.description}</p>}{c.instructor&&<div style={{fontSize:'12px',color:'var(--text-muted)',marginTop:'8px'}}>المدرب: {c.instructor}</div>}<div style={{display:'flex',justifyContent:'space-between',padding:'10px',background:'var(--bg-body)',borderRadius:'8px',marginTop:'12px'}}><span style={{fontSize:'13px'}}><BookOpen size={14} style={{display:'inline',marginLeft:'4px'}}/> {c._count?.modules||0} وحدة</span><span style={{fontSize:'13px'}}><Users size={14} style={{display:'inline',marginLeft:'4px'}}/> {c._count?.enrollments||0} متدرب</span>{c.duration>0&&<span style={{fontSize:'13px'}}>{c.duration} ساعة</span>}</div></div>))}{courses.length===0&&<div className="card" style={{padding:'40px',textAlign:'center',color:'var(--text-muted)',gridColumn:'1/-1'}}>لا توجد دورات</div>}</div>)}
  {showModal&&(<div className="modal-overlay"><div className="modal-content" style={{maxWidth:'500px'}}><div className="modal-header"><h2>دورة جديدة</h2><button className="btn btn-ghost" onClick={()=>setShowModal(false)}>✕</button></div><div className="modal-body"><form onSubmit={save}><div className="grid-2"><div className="input-group" style={{gridColumn:'1/-1'}}><label className="input-label">اسم الدورة*</label><input className="input" required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="input-group"><label className="input-label">المستوى</label><select className="input" value={form.level} onChange={e=>setForm({...form,level:e.target.value})}><option value="BEGINNER">مبتدئ</option><option value="INTERMEDIATE">متوسط</option><option value="ADVANCED">متقدم</option></select></div><div className="input-group"><label className="input-label">المدة (ساعة)</label><input className="input" type="number" value={form.duration} onChange={e=>setForm({...form,duration:e.target.value})}/></div><div className="input-group"><label className="input-label">المدرب</label><input className="input" value={form.instructor} onChange={e=>setForm({...form,instructor:e.target.value})}/></div><div className="input-group"><label className="input-label">التصنيف</label><input className="input" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></div><div className="input-group" style={{gridColumn:'1/-1'}}><label className="input-label">الوصف</label><textarea className="input" rows={3} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div></div><div style={{display:'flex',justifyContent:'flex-end',gap:'12px',marginTop:'20px'}}><button type="button" className="btn btn-ghost" onClick={()=>setShowModal(false)}>إلغاء</button><button type="submit" className="btn btn-primary">حفظ</button></div></form></div></div></div>)}</div>);
}
