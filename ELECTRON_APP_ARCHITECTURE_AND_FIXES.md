# الدليل الشامل لهيكلة وبناء تطبيق سطح المكتب (Electron + Next.js + PostgreSQL)

هذا الملف يوثق كافة المتطلبات، الشروط، والإصلاحات الجوهرية التي تم تنفيذها على تطبيق `Nama Invest ERP` لسطح المكتب. **يجب الرجوع إلى هذا الملف والالتزام بما فيه عند إنشاء أي تطبيق جديد أو إجراء صيانة جذرية للنسخة الحالية.**

---

## 1. المعمارية العامة (Architecture)
التطبيق مبني بنظام **الهجين (Hybrid)**، ويتكون من:
1. **واجهة أمامية وخلفية (Next.js Standalone):** يتم بناء التطبيق باستخدام `Next.js` في وضع `standalone` ليكون مستقلاً ولا يحتاج إلى تحميل `node_modules` كاملة.
2. **محرك قاعدة البيانات (Embedded PostgreSQL):** يتم تشغيل خادم `PostgreSQL` مدمج داخل بيئة العميل، على المنفذ `5433` (لتجنب التعارض مع أي خادم محلي آخر).
3. **مدير النوافذ (Electron):** يعمل كغلاف (Wrapper) يطلق قاعدة البيانات، ينتظر إقلاع `Next.js`، ثم يعرض الواجهة في نافذة متصفح مدمجة.

---

## 2. المشاكل الكبرى التي واجهتنا وحلولها الجذرية (الدروس المستفادة)

### أ. تعليق التطبيق أو إغلاقه بصمت (Silent Crashes)
**المشكلة:** عند حدوث أي خطأ برمجي غير متوقع في `Next.js` أو `Electron`، كان التطبيق يُغلق دون أي رسالة توضيحية للعميل، وكان كود إرسال الأخطاء القديم يعلق بسبب استخدام طرق متزامنة (Synchronous) أو وعود (Promises) غير معالجة.
**الحل المعتمد:**
- الاعتماد الكامل على أحداث `uncaughtException` و `unhandledRejection` في ملف `electron/main.js`.
- إظهار رسالة خطأ واضحة للعميل باستخدام `dialog.showErrorBox`.
- إرسال التقرير برمجياً إلى السيرفر (عبر `fetch` غير متزامن) مع تحديد **مهلة زمنية قصوى (Timeout)** مقدارها 3 ثوانٍ حتى لا يعلق البرنامج.
- تمرير `process.exit(1)` بعد إظهار الخطأ لضمان إغلاق العملية المعلقة بشكل سليم.

### ب. خطأ فقدان حزم تشغيل الويندوز (`Visual C++ Redistributable`)
**المشكلة:** قاعدة البيانات المدمجة `PostgreSQL` تتطلب مكتبات `C++` (vcredist) لتعمل على نظام Windows. غيابها يؤدي إلى فشل بدء قاعدة البيانات على أجهزة العملاء الجديدة.
**الحل المعتمد:**
- أتمتة تحميل حزمة `vcredist_x64.exe` برمجياً في سكريبت ما قبل البناء (`prebuild.js`).
- دمج الحزمة داخل التثبيت النهائي باستخدام `extraResources` في ملف إعدادات `electron-builder.yml`.
- إجبار المثبت (Installer) على تثبيت هذه الحزمة بصمت عبر سكريبت `installer.nsh` باستخدام ماكرو `customInstall`.

### ج. مشكلة تلف ملف `package.json` وتوقف `Next.js` المفاجئ (ERR_INVALID_PACKAGE_CONFIG)
**المشكلة:** أثناء عملية تغليف التطبيق وضغطه بـ `asar` أو خلال تشفير الأكواد، كان ملف `package.json` داخل مجلد `standalone` يتعرض للفساد بسبب تضمين بيانات ثنائية/مشفرة لا يتعرف عليها Node.js.
**الحل المعتمد:**
- كتابة سكريبت `after-pack.js` يعمل في اللحظة الأخيرة بعد تجميع `Next.js`.
- يقوم هذا السكريبت بحذف ملف `package.json` القديم وبناء ملف `package.json` **جديد ونظيف** كلياً (Hardcoded) لضمان أن `Node.js` يستطيع قراءته وتحديد ملف الإقلاع (`server.js`).

### د. أخطاء توافق Prisma Migration (Generator Build)
**المشكلة:** محاولة تنفيذ أمر `prisma db push` لتحديث جداول قاعدة البيانات عند تشغيل البرنامج لدى العميل كانت تؤدي لخطأ فشل بناء المولدات (`generator-build`).
**الحل المعتمد:**
- استخدام علم `--skip-generate` بشكل صريح في إعدادات `local-postgres.js` ليتم تخطي مرحلة بناء الأنواع المتوافقة في بيئة الإنتاج، لأنها مطلوبة فقط أثناء التطوير.

### هـ. مشاكل بذر البيانات الأولية (Seeding) والتوافق مع الـ Schema
**المشكلة:** السكريبت الخاص بإدخال بيانات المدير الأولية (`admin / admin`) ووحدات القياس كان مبرمجاً بلغة `SQL` الخام بأسماء جداول قديمة (مثل `"User"` بدلاً من `users`، و `password` بدلاً من `password_hash`).
**الحل المعتمد:**
- تم تعديل كافة أوامر الاستعلامات (Queries) داخل `local-postgres.js` في دالة `seedDefaults()` لتتطابق تماماً مع أسماء الجداول في `schema.prisma` المعتمدة لـ PostgreSQL (`users`, `stocks`, `units`, `customers`, `accounts`).

---

## 3. الشروط الواجب توفرها عند بناء تطبيق جديد

عند طلب بناء تطبيق Electron جديد لنفس النظام، يجب على الذكاء الاصطناعي مراجعة وتضمين هذه الملفات/الشروط:
1. **`prebuild.js`**: يجب أن يقوم بتنزيل الاعتمادات المسبقة (مثل vcredist أو Java لـ ZATCA).
2. **`installer.nsh`**: يجب تضمينه لتنفيذ تثبيت صامت للبيئات الضرورية أثناء التثبيت.
3. **`after-pack.js`**: إلزامي لمنع تلف `package.json` للـ Standalone وإزالة الملفات الضخمة غير اللازمة (مثل مجلد `.next/cache`).
4. **`electron/main.js`**: يجب أن يحتوي على هيكل معالجة الأخطاء وإرسالها للسيرفر (Crash Reporter) دون تعليق النظام.
5. **`local-postgres.js`**: استدعاء `prisma db push --skip-generate` وبذر البيانات بالـ Schema المُحدّثة للإنتاج (`snake_case` كما هو محدد بالـ `@map`).

## 4. متغيرات البيئة أثناء البناء
لا تنسَ دائماً تمرير متغير `ELECTRON_BUILD=1` أثناء أوامر التجميع الخاصة بـ `Next.js` لتوجيه الإخراج إلى مجلدات منفصلة ولتمكين خصائص تتوافق مع بيئة سطح المكتب.

**أمر التجميع المرجعي:**
```bash
node scripts/prebuild.js && cross-env ELECTRON_BUILD=1 next build && cross-env ELECTRON_BUILD=1 node scripts/clean-standalone.js && cross-env ELECTRON_BUILD=1 node scripts/protect-code.js && cross-env ELECTRON_BUILD=1 electron-builder --win
```

## 5. المعالجة التلقائية لانهيار قاعدة البيانات (Automated Reset)
في تحديثات المستقبل، يجب ألا نعتمد على العميل للقيام بحذف مجلد `pgdata` يدوياً من `%APPDATA%\namaweb` عند وجود أخطاء في الـ Schema أو فشل في الإعدادات الافتراضية.
**الحلول المعتمدة للمستقبل:**
1. **التحقق الذاتي في `local-postgres.js`**: كتابة كود يفحص ما إذا كانت الجداول الأساسية موجودة أو إذا كان حجم قاعدة البيانات غير منطقي، وإذا اكتشف خللاً هيكلياً (Corrupted Database) يقوم بمسح مجلد `pgdata` برمجياً وإعادة تهيئته (Re-init) دون تدخل المستخدم.
2. **زر إعادة ضبط المصنع (Factory Reset)**: توفير زر في واجهة الطوارئ (Crash UI) يُمكّن العميل من "إعادة ضبط النظام" بنقرة واحدة، حيث يقوم البرنامج بإغلاق خادم `Postgres`، مسح المجلد، وإعادة تشغيل التطبيق ليقوم بالبناء من الصفر.

## 6. المتطلبات الصارمة للنسخة المكتبية (Desktop Strict Requirements)
لضمان عمل النسخة المكتبية (Standalone Electron) بشكل صحيح وبدون تداخل مع نسخة السحابة (Web/SaaS)، يجب الالتزام الدائم بالشروط التالية في الكود:
1. **استبعاد Clerk وتجنب تداخل شاشات الدخول:**
   - **الشرط:** النسخة المكتبية يجب ألا تُظهر أو تُعالج أي مكونات لـ Clerk (مثل `<SignIn>` أو زر التسجيل بجوجل).
   - **الآلية المتبعة:** يتم حقن المتغير `NEXT_PUBLIC_IS_DESKTOP=1` أثناء أمر البناء (`ELECTRON_BUILD=1`) عبر `next.config.ts`.
   - يتم تمرير هذا المتغير في صفحة `login/page.tsx` للتهيئة الأولى (`useState`) لضمان أن السيرفر (SSR) والعميل متزامنان ولا يتم عرض أي مكون Clerk إطلاقاً.
2. **المزامنة التلقائية والمبكرة في لوحة التحكم (ICE Panel):**
   - **الشرط:** بمجرد قيام العميل بتثبيت النسخة المكتبية لأول مرة، يجب أن يظهر جهازه فوراً في لوحة تحكم التراخيص `ICE`، حتى لو لم يقم العميل بإعداد "اسم الشركة" بعد.
   - **الآلية المتبعة:** في سكربت `electron/main.js` داخل دالة `syncLicenseToCloud()`، لا تقم أبداً بإنهاء العملية (Abort) إذا كان اسم الشركة الافتراضي (`اسم المنشأة`) أو فارغاً. بل استبدله باسم افتراضي مثل `منشأة جديدة (قيد الإعداد)` وأرسل طلب التسجيل ليظهر في لوحة التحكم فوراً.
3. **تجنب مشاكل التصميم في النسخة المكتبية (Triangles/Select Backgrounds):**
   - **الشرط:** لمنع ظهور الأيقونة المنسدلة الافتراضية بشكل متكرر (المثلثات) في كافة الثيمات والشاشات (خاصة في `theme-white`) في التطبيق المكتبي أو الموقع.
   - **الآلية المتبعة:** أضف دائماً `select { background-repeat: no-repeat !important; }` كحماية إضافية (Global Failsafe) في ملف `globals.css`.
