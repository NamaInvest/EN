# خطة إصلاح نظام ضبط المصنع (Factory Reset Remediation)

بعد الفحص العميق لملف `src/app/api/system/reset/route.ts`، تبين أن زر "فرمتة وتهيئة النظام" يعاني من 3 أخطاء برمجية كارثية تمنعه من العمل، بل وتهدد بانهيار واجهة النظام (Lockout) إذا نُفذ جزئياً.

## ⚠️ الأخطاء المكتشفة (Root Causes)

1. **حظر أصحاب النظام (403 Forbidden):**
   * الباك-إند يرفض العملية لأي مستخدم ليس دوره `admin` حرفياً. إذا كان المستخدم `owner` (مالك)، فإن الباك-إند يرفض العملية رغم ظهور الزر له في لوحة التحكم.
2. **محو صلاحيات المدير العام (Admin Lockout):**
   * كود الـ (TRUNCATE CASCADE) يقوم بفرمتة الجداول، ولكنه نسي استثناء جدول `user_permissions`. مما يعني أن صلاحياتك أنت كـ (Owner) ستُحذف، وعند تسجيل دخولك مرة أخرى ستجد لوحة التحكم بيضاء بدون أي قوائم.
3. **انهيار نقاط البيع (Missing ID=1 Defaults):**
   * حذف جدول `units` (الوحدات) و `stocks` (المستودعات) سيؤدي لانهيار شاشة نقاط البيع والمبيعات لأن النظام يعتمد افتراضياً على وجود (مستودع رئيسي `id=1`) و(وحدة حبة `id=1`) كعناصر ارتكازية.

---

## 🛠️ التغييرات المقترحة (Proposed Changes)

سنقوم بتعديل ملف الباك-إند الجذرى لزر הפرمتة لضمان الفرمتة الآمنة.

#### [MODIFY] `src/app/api/system/reset/route.ts`
- **التعديل الأول:** توسيع صلاحيات الباك-إند لتشمل الـ Owner والـ Admin.
  `if (!user || !['admin', 'owner'].includes(user.role))`
- **التعديل الثاني:** إضافة `'user_permissions'` إلى قائمة الاستثناءات `excluded` لتجنب طرد المالك من الصلاحيات.
- **التعديل الثالث:** إضافة أكواد الحماية الآلية (Re-seeding) لضمان عدم تعطل النظام بعد الفرمتة:
  - زرع مستودع افتراضي (`id: 1, name: 'المستودع الرئيسي'`).
  - زرع وحدة قياس افتراضية (`id: 1, name: 'حبة'`).
  - التأكد من زرع العميل الافتراضي لتجنب شلل نقاط البيع.

---

## 🔍 خطة التحقق المبدئية (Verification Plan)
بعد تطبيق هذا التعديل، سنقوم بـ:
1. الضغط على زر **تهيئة النظام** في N11 عبر حساب Owner.
2. التحقق من بقاء صلاحيات المالك كما هي لعدم حظره من واجهته.
3. محاولة إجراء فاتورة POS نقدية للتأكد من أن المستودع الافتراضي ووحدة القياس الافتراضية ورقم العميل النقدي تمت إعادة زراعتها بنجاح تام.
