# ARCHITECTURE WARNING — `compliance/pdpl/breaches`

> Generated by Gate 1 verification (per AI_EXECUTION_STANDARD v2.0)
> Date: 2026-05-19
> Target: `src/app/(dashboard)/compliance/pdpl/breaches`

---

## 📋 ملخص الحالة

| المسار | الحالة |
|---|---|
| Page UI: `src/app/(dashboard)/compliance/pdpl/breaches/page.tsx` | ❌ Placeholder فقط (`FeatureDisabledPanel`) |
| API: `src/app/api/pdpl/breach/route.ts` | ⚠️ موجود لكن فيه ضعف معماري |
| Prisma model: `PdplBreachIncident` | ✅ مكتمل |
| Engine: `src/lib/pdpl-engine.ts` | ✅ مكتمل (recordBreach, getActiveBreaches) |
| Service: `src/services/shared/pdpl.service.ts` | ✅ موجود |

---

## ⚠️ مخالفات Gate 1 على API الحالي

| # | المخالفة | الملف:السطر | الإصلاح |
|--:|---|---|---|
| 1 | استخدام `(req as any)` و `(user as any).id` | `route.ts:17, 37, 53` | استخدام `RouteContext` من `withRoute` بشكل صحيح |
| 2 | Zod schema ضعيف — `z.any()` لكل الحقول | `route.ts:30-34` | تعريف enums صريحة لـ category/severity + strict types |
| 3 | لا يوجد RBAC permission check (auth فقط) | `route.ts:17, 37` | إضافة `roles: ['admin', 'compliance_officer']` |
| 4 | لا يوجد `logAuditAction` call | route كاملاً | إضافة audit log لـ POST |
| 5 | لا يوجد observability trace | route كاملاً | إضافة `logger.financial` style trace |
| 6 | فقط GET + POST — لا UPDATE لـ status changes | كامل الـ folder | إضافة `[id]/route.ts` لـ PATCH/PUT |
| 7 | الـ engine يستدعي `db(prisma)` بدل types صريحة | `pdpl-engine.ts` | acceptable — wrapper موجود لأسباب type-safety |
| 8 | الـ UI page يشير لـ `/api/compliance/pdpl/breaches` خطأ | `page.tsx:8` | الـ API الفعلي هو `/api/pdpl/breach` |

---

## 🔧 خطة الإصلاح (Vertical Completion)

### Step 1: تحديث `src/app/api/pdpl/breach/route.ts`
- استخدام `RouteContext` بشكل صحيح
- Strict Zod enums لـ category/severity
- إضافة RBAC `roles: ['admin', 'compliance_officer']`
- إضافة `logAuditAction` بعد POST
- إضافة logger.child traces لكل عملية
- إضافة pagination + filtering للـ GET
- التوثيق الشامل لكل function بـ JSDoc

### Step 2: إنشاء `src/app/api/pdpl/breach/[id]/route.ts`
- PATCH endpoint لتحديث status (DETECTED → CONTAINED → INVESTIGATING → RESOLVED → CLOSED)
- POST `/notify-sdaia` لتسجيل تبليغ SDAIA
- GET للتفاصيل الكاملة

### Step 3: بناء `src/app/(dashboard)/compliance/pdpl/breaches/page.tsx`
- استبدال FeatureDisabledPanel بالكامل
- تطبيق 15 ميزة Enterprise UX
- توثيق شامل بـ JSDoc + inline comments
- RTL + dark mode + responsive

### Step 4: إضافة i18n keys
- `src/locales/ar.json` و `src/locales/en.json`
- تحت namespace: `pdpl.breaches.*`

### Step 5: DoD verification
- 15-item checklist

---

## ✅ ما يمكن إعادة استخدامه (بدون تعديل)

- `src/lib/pdpl-engine.ts` → `recordBreach`, `getActiveBreaches`
- `src/lib/audit.ts` → `logAuditAction`
- `src/lib/api/with-route.ts` → wrapper مع RBAC support (`roles: [...]`)
- `src/lib/governance/tenant-guard.ts` → tenant isolation
- `src/lib/observability/financial-trace.ts` → tracing wrapper (للعمليات الحرجة)
- `src/components/DataTable.tsx` → جدول مع sort/filter/pagination/CSV export
- `src/components/Toast.tsx` → `useToast()` للإشعارات
- `src/components/ui/{card,button,dialog,select,input,label,badge}.tsx` → shadcn primitives
- `src/lib/i18n.tsx` → `useTranslation()`

---

## 🚦 القرار النهائي

**Build مع إصلاح موازي**:
- الـ API يحتاج تحسين لكن وظيفته موجودة
- سأبني الـ UI **و** أحدث الـ API في نفس الجلسة (vertical completion صحيح)
- الـ engine + model + service موجودون بشكل ممتاز

**ابدأ التنفيذ.**
