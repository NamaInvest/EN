const { Client } = require('ssh2'); const c = new Client(); c.on('ready', () => { c.exec('grep -ril "ط·" /www/wwwroot/n11.namainvist.com/src/', (err, stream) => { let out=''; stream.on('close', () => c.end()).on('data', d => out+=d.toString()).on('end', () => console.log('CORRUPTED FILES:', out)); }); }).connect({host:'46.4.188.170',port: 22,username: 'root',password: '_ee4SWbxLVfH9b'});
